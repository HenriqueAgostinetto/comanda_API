version: '3.8'
services:
  db:
    image: mysql:8.0
    environment:
      MYSQL_ROOT_PASSWORD: rootpassword
      MYSQL_DATABASE: comanda_db
    ports:
      - "3306:3306"
  
  api:
    build: .
    volumes:
      - .:/app
    ports:
      - "4443:8000"
    depends_on:
      - db